---
import { Leaf } from "@lucide/astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/Section.astro";
import { getProfile } from "../lib/profile";

const profile = getProfile();
const siteUrl = "https://www.bellingard.fr";
---

<BaseLayout
  title={`Low-impact site — ${profile.name}`}
  description="This website is built to keep its carbon footprint as low as possible: static pages, minimal assets, and no unnecessary scripts."
  canonicalPath="/low-impact"
>
  <Section
    id="low-impact"
    eyebrow="Sustainability"
    icon={Leaf}
    title="Built for a low carbon footprint"
    intro="This site is a static Astro build — lean pages, few requests, and no trackers — so each visit costs as little energy as possible. Independent tools measure that footprint below."
    tone="snow"
  >
    <ul class="grid gap-10 sm:grid-cols-2">
      <li class="max-w-md">
        <h3 class="font-display text-lg font-semibold text-forest">EcoIndex</h3>
        <p class="mt-2 text-base leading-7 text-ink/70">
          EcoIndex rates environmental performance from A to G, based on page weight, DOM complexity, and HTTP requests.
        </p>
        <div class="mt-5">
          <a href={`https://bff.ecoindex.fr/redirect/?url=${siteUrl}`} target="_blank" rel="noopener noreferrer">
            <img
              src={`https://bff.ecoindex.fr/badge/?theme=light&url=${siteUrl}`}
              alt="EcoIndex Badge"
              width="100"
              height="43"
              loading="lazy"
              decoding="async"
            />
          </a>
        </div>
      </li>
      <li class="max-w-md">
        <h3 class="font-display text-lg font-semibold text-forest">Website Carbon</h3>
        <p class="mt-2 text-base leading-7 text-ink/70">
          The Website Carbon badge estimates CO₂ emissions per page view and how that compares with other sites.
        </p>
        <div class="mt-5">
          <div id="wcb" class="carbonbadge"></div>
          <script is:inline src="https://unpkg.com/website-carbon-badges@1.1.3/b.min.js" defer></script>
        </div>
      </li>
    </ul>
  </Section>
</BaseLayout>
